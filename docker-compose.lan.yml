# Publish backend APIs on all interfaces (LAN / company server).
# Do not also add docker-compose.local.yml (Compose would bind the same port twice).

services:
  hub-platform:
    ports:
      - "8090:8090"

  pkm-backend:
    ports:
      - "8001:8000"
