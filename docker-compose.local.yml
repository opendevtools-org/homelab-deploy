# Publish backend APIs on localhost only (this machine).
# Do not also add docker-compose.lan.yml.

services:
  hub-platform:
    ports:
      - "127.0.0.1:8090:8090"

  pkm-backend:
    ports:
      - "127.0.0.1:8001:8000"
