# Site CLI commands. This folder is site-owned; Update-HomelabUpstream does not
# overwrite it. Copy scriptkit/examples/echo_cli/ to cli/<name>/src/ when adding
# a tool. See agent-context/pkm/SCRIPTS.md.
